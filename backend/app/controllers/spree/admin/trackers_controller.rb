module Spree
  module Admin
    class TrackersController < ResourceController
    	#Is this like a master class to keep the complexity of the controller to less while still allowing the controller to have all the necessary controls?
    end
  end
end
